{{ form_start(form) }}
{{ form_widget(form.type, {
        'attr': {
        'class': 'd-none'
        }}) }}
{{ form_row(form.title) }}
<a href="{{ path('admin_service_category_index') }}" class="btn btn-secondary mt-3 me-3 text-light">
	<i class="bi bi-arrow-left-square test-light me-1"></i>Retour</a>
<button class="btn btn-primary mt-3">{{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
