{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Mes expériences{% endblock %}

{% block stylesheets %}
	{{ encore_entry_link_tags('forms') }}
{% endblock %}

{% block body %}
	<main class="container">
		<h1 class="text-center mt-5 ">
			Mes expériences</h1>
		<div class="col-md-6 et offset-md-3 fs-4 mt-5">
			{{ form_start(form) }}
			<h2 class="title text-info fs-4">{{ form_widget(form) }}<h2>
					<button class="btn btn-secondary fw-bold text-light align-self-center mb-4">Valider</button>
					<a class="btn btn-outline-dark align-self-center mb-4" href=" {{ path('candidat_show', {'user': candidat.user}) }}">Retour</a>
					{{ form_end(form) }}
				</div>
			</main>
		{% endblock %}
