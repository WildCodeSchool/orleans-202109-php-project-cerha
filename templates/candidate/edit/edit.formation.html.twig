{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Mes Formations{% endblock %}
{% block stylesheets %}
	{{ encore_entry_link_tags('forms') }}
{% endblock %}
{% block body %}
	<main class="container">
		<h1 class="text-center mt-5 ">
			Formations<h1>
				<div class="col-md-6 et offset-md-3 fs-4 mt-5">
					{{ form_start(form) }}
					{{ form_widget(form) }}
					<button class="btn btn-secondary fw-bold text-light align-self-center mb-4">Valider</button>
					<a class="btn btn-outline-dark align-self-center mb-4" href=" {{ path('candidate_show', {'user': candidate.user}) }}">Retour</a>
					{{ form_end(form) }}
				</div>
			</main>
		{% endblock %}
