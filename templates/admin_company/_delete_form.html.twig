<form method="post" action="{{ path('admin_company_delete', {'id': company.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company.id) }}">
    <button class="btn btn-danger px-3 py-1"><i class="bi bi-trash text-light"></i></button>
</form>
