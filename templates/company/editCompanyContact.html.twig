{% extends "base.html.twig" %}

{% block title %}
	{{ company.denomination }}
{% endblock %}

{% block body %}
	<main class="container">
		<h1 class="text-center mt-5 ">Modifier vos informations</h1>
		<div class="col-md-6 et offset-md-3 fs-4 mt-5">
			{{ include('company/components/_formCompanyContact.html.twig', {'button_label': 'Valider'}) }}

			<a href="{{ path('company_show', {id: company.id}) }}">Annuler</a>
		</div>
	</main>
{% endblock %}
