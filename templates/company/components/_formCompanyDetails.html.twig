{{ form_start(form) }}
<div class="row mb-2">
	{{ form_row(form.denomination) }}
</div>
<div class="row mb-2">
	<div class="col-md-4">{{ form_row(form.siret) }}</div>
	<div class="col-md-4">{{ form_row(form.apeCode) }}</div>
	<div class="col-md-4">{{ form_row(form.vatNumber) }}</div>
</div>
<div class="row mb-2">
	<div class="col-md-4">{{ form_row(form.address) }}</div>
	<div class="col-md-4">{{ form_row(form.postalCode) }}</div>
	<div class="col-md-4">{{ form_row(form.city) }}</div>
</div>
<div class="row mb-2">
	<div class="col-md-6">{{ form_row(form.website) }}</div>
	<div class="col-md-6">{{ form_row(form.linkedin) }}</div>
</div>
<div class="row mb-2">
	<div class="col-md-6">{{ form_row(form.facebook) }}</div>
	<div class="col-md-6">{{ form_row(form.instagram) }}</div>
</div>
<button class="btn btn-secondary fw-bold text-light align-self-center mb-4">Valider</button>
<a class="btn btn-outline-dark align-self-center mb-4" href=" {{ path('company_show', {'id': company.id}) }}">Retour</a>
{{ form_end(form) }}
