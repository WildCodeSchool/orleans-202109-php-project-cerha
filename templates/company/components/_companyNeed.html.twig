<div class="row companyNeed mt-5 d-lg-block d-flex justify-content-center" id="companyNeed">
	<div class="card p-4 col-lg-6 col-10 offset-lg-3">
		{% if is_granted("ROLE_ADMIN") == false %}
			<a class="btn btn-outline-light ms-5 align-self-end fs-5  bi bi-pencil-square" href="{{ path('company_editNeed') }}">
				Modifier</a>
		{% endif %}
		<h2 class="card-title text-secondary text-center mt-3 pt-2 px-4">Descritpion du besoin</h2>
		<div class="mt-3 mx-3">{{ company.need|nl2br }}</div>
	</div>
</div>
