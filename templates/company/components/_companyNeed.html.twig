<div class="row companyNeed mt-5" id="companyNeed">
	<div class="card p-4 col-md-6 offset-md-3">
		{% if is_granted("ROLE_ADMIN") == false %}
			<a class="btn btn-outline-light ms-5 align-self-end fs-5  bi bi-pencil-square" href="{{ path('company_editNeed', {id: company.id}) }}">
				Modifier</a>
		{% endif %}
		<h2 class="card-title text-secondary text-center mt-3 pt-2 px-4">Descritpion du besoin</h2>
		<div class="mt-3 mx-3">{{ company.need|nl2br }}</div>
	</div>
</div>
