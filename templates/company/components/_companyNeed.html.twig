<div class="row companyNeed mt-5 d-lg-block d-flex justify-content-center" id="companyNeed">
	<div class="card p-4 col-lg-6 col-10 offset-lg-3">
		<div class="d-flex mt-2 justify-content-between">
			<h2 class="card-title text-secondary ms-3 pt-1">Description du besoin</h2>
			{% if is_granted("ROLE_ADMIN") == false %}
				<a class="btn fs-6 bi bi-pencil-square pt-2" href="{{ path('company_editNeed') }}">
					Modifier</a>
			{% endif %}
		</div>
		<div class="mt-3 mx-3">{{ company.need|nl2br }}</div>
	</div>
</div>
